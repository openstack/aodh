[build-system]
requires = ["pbr>=6.0.0", "setuptools>=64.0.0"]
build-backend = "pbr.build"

[tool.autopep8]
aggressive = 3
in-place = true
recursive = true
ignore = ["E731"]

[tool.doc8]
ignore = ["D001"]

[tool.ruff]
line-length = 79

[tool.ruff.lint]
select = [
    # mcabe complexity checks
    "C90",
    # multiple-imports-on-one-line, module-import-not-at-top-of-file
    "E4",
    # line-too-long, redundant-backslash
    "E5",
    "E7",
    "E9",
    # pyflakes
    "F",
    "G",
    "LOG",
    "S",
    "UP",
    "W",
]
external = ["H"]
ignore = [
    # UP031 % format — defer f-string migration to a later pass
    "UP031",
    # UP032 f-string — defer migration to a later pass
    "UP032",
]

[tool.ruff.lint.per-file-ignores]
"doc/*" = ["E501"]
"aodh/storage/sqlalchemy/alembic/versions/*" = ["E402"]
"aodh/tests/*" = ["S"]

[tool.ruff.lint.mccabe]
# Flag errors (`C901`) whenever the complexity level exceeds 25.
max-complexity = 25
